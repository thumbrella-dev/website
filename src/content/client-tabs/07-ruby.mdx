---
label: Ruby
---

Plain Ruby gem, no runtime dependencies beyond the standard library.

```sh
gem install thumbrella
```

```ruby
require "thumbrella"

tbr = Thumbrella.new("YOUR_KEY")
thumb = tbr.thumb("https://example.com/video.mp4")

File.write("thumb.webp", Base64.decode64(thumb.data))
```
